<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expressão Gênica - CPLP Raras</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .header {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .header h1 {
            color: #667eea;
            margin-bottom: 10px;
        }
        
        .nav-link {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 10px 20px;
            border-radius: 25px;
            text-decoration: none;
            margin: 10px 5px;
            transition: all 0.3s ease;
        }
        
        .nav-link:hover {
            background: #764ba2;
            text-decoration: none;
            color: white;
        }
        
        .content-grid {
            display: grid;
            grid-template-columns: 1fr 300px;
            gap: 20px;
        }
        
        .main-content {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .sidebar {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            height: fit-content;
        }
        
        .sidebar h3 {
            color: #667eea;
            margin-bottom: 15px;
            font-size: 1.1rem;
        }
        
        .sidebar ul {
            list-style: none;
        }
        
        .sidebar li {
            padding: 8px 0;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
        }
        
        .table-container {
            overflow-x: auto;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        
        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        
        th {
            background-color: #f8f9fa;
            font-weight: 600;
            color: #667eea;
        }
        
        .gene-id {
            font-family: 'Courier New', monospace;
            background: #f8f9fa;
            padding: 4px 8px;
            border-radius: 4px;
            font-weight: 600;
        }
        
        .expression-level {
            text-align: right;
            font-weight: 600;
        }
        
        .high-expression {
            color: #d63384;
        }
        
        .medium-expression {
            color: #fd7e14;
        }
        
        .low-expression {
            color: #6c757d;
        }
        
        .tissue-badge {
            background: #e9ecef;
            color: #495057;
            padding: 4px 8px;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: 500;
        }
        
        .brain {
            background: #d4edda;
            color: #155724;
        }
        
        .liver {
            background: #fff3cd;
            color: #856404;
        }
        
        .heart {
            background: #f8d7da;
            color: #721c24;
        }
        
        .muscle {
            background: #cce5ff;
            color: #004085;
        }
        
        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            margin-top: 20px;
        }
        
        .pagination a, .pagination span {
            padding: 8px 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            text-decoration: none;
            color: #667eea;
        }
        
        .pagination .current {
            background: #667eea;
            color: white;
            border-color: #667eea;
        }
        
        .pagination a:hover {
            background: #f0f0f0;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📊 Expressão Gênica</h1>
            <p><%= total.toLocaleString() %> perfis de expressão do Expression Atlas</p>
            <div>
                <a href="/" class="nav-link">🏠 Início</a>
                <a href="/ensembl-genes" class="nav-link">🧬 Genes</a>
                <a href="/uniprot-proteins" class="nav-link">🧬 Proteínas</a>
                <a href="/ema-medicines" class="nav-link">💊 EMA</a>
            </div>
        </div>

        <div class="content-grid">
            <div class="main-content">
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>Gene ID</th>
                                <th>Tecido/Célula</th>
                                <th>Condição</th>
                                <th>Nível de Expressão</th>
                                <th>P-valor</th>
                                <th>Experimento</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% expressions.forEach(expr => { %>
                            <tr>
                                <td><span class="gene-id"><%= expr.gene_id %></span></td>
                                <td>
                                    <span class="tissue-badge <%= expr.tissue_type.toLowerCase().includes('brain') ? 'brain' : expr.tissue_type.toLowerCase().includes('liver') ? 'liver' : expr.tissue_type.toLowerCase().includes('heart') ? 'heart' : expr.tissue_type.toLowerCase().includes('muscle') ? 'muscle' : '' %>">
                                        <%= expr.tissue_type %>
                                    </span>
                                </td>
                                <td><%= expr.condition %></td>
                                <td class="expression-level <%= expr.expression_level > 10 ? 'high-expression' : expr.expression_level > 5 ? 'medium-expression' : 'low-expression' %>">
                                    <%= expr.expression_level.toFixed(2) %>
                                </td>
                                <td><%= expr.p_value ? expr.p_value.toExponential(2) : 'N/A' %></td>
                                <td><%= expr.experiment_id %></td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>

                <div class="pagination">
                    <% if (currentPage > 1) { %>
                        <a href="?page=<%= currentPage - 1 %>">‹ Anterior</a>
                    <% } %>
                    
                    <span class="current">Página <%= currentPage %> de <%= totalPages %></span>
                    
                    <% if (currentPage < totalPages) { %>
                        <a href="?page=<%= currentPage + 1 %>">Próxima ›</a>
                    <% } %>
                </div>
            </div>

            <div class="sidebar">
                <h3>🧠 Por Tecido</h3>
                <ul>
                    <% tissues.slice(0, 10).forEach(tissue => { %>
                    <li>
                        <span><%= tissue.tissue_type.length > 20 ? tissue.tissue_type.substring(0, 20) + '...' : tissue.tissue_type %></span>
                        <span><%= Number(tissue.count).toLocaleString() %></span>
                    </li>
                    <% }); %>
                </ul>

                <h3 style="margin-top: 25px;">🧪 Por Condição</h3>
                <ul>
                    <% conditions.slice(0, 8).forEach(condition => { %>
                    <li>
                        <span><%= condition.condition.length > 20 ? condition.condition.substring(0, 20) + '...' : condition.condition %></span>
                        <span><%= Number(condition.count).toLocaleString() %></span>
                    </li>
                    <% }); %>
                </ul>

                <h3 style="margin-top: 25px;">📈 Níveis de Expressão</h3>
                <ul>
                    <% expressionRanges.forEach(range => { %>
                    <li>
                        <span><%= range.range %></span>
                        <span><%= Number(range.count).toLocaleString() %></span>
                    </li>
                    <% }); %>
                </ul>

                <div style="margin-top: 25px; padding: 15px; background: #f8f9fa; border-radius: 10px;">
                    <h4 style="color: #667eea; margin-bottom: 10px;">📊 Estatísticas</h4>
                    <p><strong>Total:</strong> <%= total.toLocaleString() %></p>
                    <p><strong>Tecidos:</strong> <%= tissues.length %></p>
                    <p><strong>Condições:</strong> <%= conditions.length %></p>
                    <p><strong>Expr. média:</strong> <%= avgExpression.toFixed(2) %></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>