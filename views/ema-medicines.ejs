<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Medicamentos EMA - CPLP Raras</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .header {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .header h1 {
            color: #667eea;
            margin-bottom: 10px;
        }
        
        .nav-link {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 10px 20px;
            border-radius: 25px;
            text-decoration: none;
            margin: 10px 5px;
            transition: all 0.3s ease;
        }
        
        .nav-link:hover {
            background: #764ba2;
            text-decoration: none;
            color: white;
        }
        
        .content-grid {
            display: grid;
            grid-template-columns: 1fr 300px;
            gap: 20px;
        }
        
        .main-content {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .sidebar {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            height: fit-content;
        }
        
        .sidebar h3 {
            color: #667eea;
            margin-bottom: 15px;
            font-size: 1.1rem;
        }
        
        .sidebar ul {
            list-style: none;
        }
        
        .sidebar li {
            padding: 8px 0;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
        }
        
        .table-container {
            overflow-x: auto;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        
        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        
        th {
            background-color: #f8f9fa;
            font-weight: 600;
            color: #667eea;
        }
        
        .orphan-badge {
            background: #fff3cd;
            color: #856404;
            padding: 4px 8px;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: 500;
        }
        
        .authorized {
            background: #d4edda;
            color: #155724;
        }
        
        .withdrawn {
            background: #f8d7da;
            color: #721c24;
        }
        
        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            margin-top: 20px;
        }
        
        .pagination a, .pagination span {
            padding: 8px 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            text-decoration: none;
            color: #667eea;
        }
        
        .pagination .current {
            background: #667eea;
            color: white;
            border-color: #667eea;
        }
        
        .pagination a:hover {
            background: #f0f0f0;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>💊 Medicamentos EMA</h1>
            <p><%= total.toLocaleString() %> medicamentos aprovados pela European Medicines Agency</p>
            <div>
                <a href="/" class="nav-link">🏠 Início</a>
                <a href="/ensembl-genes" class="nav-link">🧬 Genes</a>
                <a href="/eu-trials" class="nav-link">🧪 Ensaios</a>
                <a href="/who-data" class="nav-link">🌍 WHO</a>
            </div>
        </div>

        <div class="content-grid">
            <div class="main-content">
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>Nome do Medicamento</th>
                                <th>Área Terapêutica</th>
                                <th>Órfão</th>
                                <th>Status</th>
                                <th>Autorização</th>
                                <th>Titular</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% medicines.forEach(medicine => { %>
                            <tr>
                                <td><strong><%= medicine.medicine_name %></strong></td>
                                <td><%= medicine.therapeutic_area %></td>
                                <td>
                                    <% if (medicine.orphan_designation) { %>
                                        <span class="orphan-badge">🦄 ÓRFÃO</span>
                                    <% } else { %>
                                        -
                                    <% } %>
                                </td>
                                <td>
                                    <span class="orphan-badge <%= medicine.marketing_status === 'Authorised' ? 'authorized' : 'withdrawn' %>">
                                        <%= medicine.marketing_status %>
                                    </span>
                                </td>
                                <td><%= new Date(medicine.authorization_date).toLocaleDateString('pt-BR') %></td>
                                <td><%= medicine.marketing_holder %></td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>

                <div class="pagination">
                    <% if (currentPage > 1) { %>
                        <a href="?page=<%= currentPage - 1 %>">‹ Anterior</a>
                    <% } %>
                    
                    <span class="current">Página <%= currentPage %> de <%= totalPages %></span>
                    
                    <% if (currentPage < totalPages) { %>
                        <a href="?page=<%= currentPage + 1 %>">Próxima ›</a>
                    <% } %>
                </div>
            </div>

            <div class="sidebar">
                <h3>🏥 Por Área Terapêutica</h3>
                <ul>
                    <% areas.slice(0, 10).forEach(area => { %>
                    <li>
                        <span><%= area.therapeutic_area %></span>
                        <span><%= Number(area.count).toLocaleString() %></span>
                    </li>
                    <% }); %>
                </ul>

                <h3 style="margin-top: 25px;">🦄 Medicamentos Órfãos</h3>
                <ul>
                    <% orphanStats.forEach(stat => { %>
                    <li>
                        <span><%= stat.orphan_designation ? 'Órfãos' : 'Não-órfãos' %></span>
                        <span><%= Number(stat.count).toLocaleString() %></span>
                    </li>
                    <% }); %>
                </ul>

                <div style="margin-top: 25px; padding: 15px; background: #f8f9fa; border-radius: 10px;">
                    <h4 style="color: #667eea; margin-bottom: 10px;">📊 Estatísticas</h4>
                    <p><strong>Total:</strong> <%= total.toLocaleString() %></p>
                    <p><strong>Órfãos:</strong> <%= Math.round((orphanStats.find(s => s.orphan_designation) ? Number(orphanStats.find(s => s.orphan_designation).count) : 0) / total * 100) %>%</p>
                    <p><strong>Áreas:</strong> <%= areas.length %></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>