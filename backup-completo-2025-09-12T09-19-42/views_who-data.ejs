<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dados WHO - CPLP Raras</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .header {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .header h1 {
            color: #667eea;
            margin-bottom: 10px;
        }
        
        .nav-link {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 10px 20px;
            border-radius: 25px;
            text-decoration: none;
            margin: 10px 5px;
            transition: all 0.3s ease;
        }
        
        .nav-link:hover {
            background: #764ba2;
            text-decoration: none;
            color: white;
        }
        
        .content-grid {
            display: grid;
            grid-template-columns: 1fr 300px;
            gap: 20px;
        }
        
        .main-content {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .sidebar {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            height: fit-content;
        }
        
        .sidebar h3 {
            color: #667eea;
            margin-bottom: 15px;
            font-size: 1.1rem;
        }
        
        .sidebar ul {
            list-style: none;
        }
        
        .sidebar li {
            padding: 8px 0;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
        }
        
        .table-container {
            overflow-x: auto;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        
        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        
        th {
            background-color: #f8f9fa;
            font-weight: 600;
            color: #667eea;
        }
        
        .indicator-badge {
            padding: 4px 8px;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: 500;
            background: #e9ecef;
            color: #495057;
        }
        
        .health {
            background: #d4edda;
            color: #155724;
        }
        
        .mortality {
            background: #f8d7da;
            color: #721c24;
        }
        
        .disease {
            background: #fff3cd;
            color: #856404;
        }
        
        .vaccination {
            background: #cce5ff;
            color: #004085;
        }
        
        .value-cell {
            text-align: right;
            font-weight: 600;
        }
        
        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            margin-top: 20px;
        }
        
        .pagination a, .pagination span {
            padding: 8px 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            text-decoration: none;
            color: #667eea;
        }
        
        .pagination .current {
            background: #667eea;
            color: white;
            border-color: #667eea;
        }
        
        .pagination a:hover {
            background: #f0f0f0;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🌍 Dados WHO</h1>
            <p><%= total.toLocaleString() %> indicadores de saúde da Organização Mundial da Saúde</p>
            <div>
                <a href="/" class="nav-link">🏠 Início</a>
                <a href="/ensembl-genes" class="nav-link">🧬 Genes</a>
                <a href="/ema-medicines" class="nav-link">💊 EMA</a>
                <a href="/eu-trials" class="nav-link">🧪 Ensaios</a>
            </div>
        </div>

        <div class="content-grid">
            <div class="main-content">
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>Indicador de Saúde</th>
                                <th>País</th>
                                <th>Categoria</th>
                                <th>Ano</th>
                                <th>Valor</th>
                                <th>Unidade</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% healthData.forEach(data => { %>
                            <tr>
                                <td><strong><%= data.indicator_name || 'N/A' %></strong></td>
                                <td><%= data.country_name || 'N/A' %></td>
                                <td>
                                    <span class="indicator-badge <%= (data.category || '').toLowerCase() %>">
                                        <%= data.category || 'N/A' %>
                                    </span>
                                </td>
                                <td><%= data.year || 'N/A' %></td>
                                <td class="value-cell"><%= data.value ? data.value.toLocaleString() : 'N/A' %></td>
                                <td><%= data.unit_measure || 'N/A' %></td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>

                <div class="pagination">
                    <% if (currentPage > 1) { %>
                        <a href="?page=<%= currentPage - 1 %>">‹ Anterior</a>
                    <% } %>
                    
                    <span class="current">Página <%= currentPage %> de <%= totalPages %></span>
                    
                    <% if (currentPage < totalPages) { %>
                        <a href="?page=<%= currentPage + 1 %>">Próxima ›</a>
                    <% } %>
                </div>
            </div>

            <div class="sidebar">
                <h3>📊 Por Categoria</h3>
                <ul>
                    <% categories.forEach(category => { %>
                    <li>
                        <span><%= category.category %></span>
                        <span><%= Number(category.count).toLocaleString() %></span>
                    </li>
                    <% }); %>
                </ul>

                <h3 style="margin-top: 25px;">🌍 Por País</h3>
                <ul>
                    <% countries.slice(0, 10).forEach(country => { %>
                    <li>
                        <span><%= country.country_name || 'N/A' %></span>
                        <span><%= Number(country.count).toLocaleString() %></span>
                    </li>
                    <% }); %>
                </ul>

                <h3 style="margin-top: 25px;">📅 Por Ano</h3>
                <ul>
                    <% years.slice(-8).forEach(year => { %>
                    <li>
                        <span><%= year.year %></span>
                        <span><%= Number(year.count).toLocaleString() %></span>
                    </li>
                    <% }); %>
                </ul>

                <div style="margin-top: 25px; padding: 15px; background: #f8f9fa; border-radius: 10px;">
                    <h4 style="color: #667eea; margin-bottom: 10px;">📈 Estatísticas</h4>
                    <p><strong>Total:</strong> <%= total.toLocaleString() %></p>
                    <p><strong>Categorias:</strong> <%= categories.length %></p>
                    <p><strong>Países:</strong> <%= countries.length %></p>
                    <p><strong>Anos:</strong> <%= years.length %></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>